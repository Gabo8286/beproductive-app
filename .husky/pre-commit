#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit quality checks..."

# Colors for output
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'

# Track if any checks fail
FAILED=0

echo -e "${BLUE}Step 1/7: Running ESLint...${NC}"
if ! npm run lint; then
    echo -e "${RED}❌ ESLint failed${NC}"
    FAILED=1
else
    echo -e "${GREEN}✅ ESLint passed${NC}"
fi

echo -e "${BLUE}Step 2/7: Running TypeScript check...${NC}"
if ! npm run type-check; then
    echo -e "${RED}❌ TypeScript check failed${NC}"
    FAILED=1
else
    echo -e "${GREEN}✅ TypeScript check passed${NC}"
fi

echo -e "${BLUE}Step 3/7: Running unit tests...${NC}"
if ! npm run test:run; then
    echo -e "${RED}❌ Unit tests failed${NC}"
    FAILED=1
else
    echo -e "${GREEN}✅ Unit tests passed${NC}"
fi

echo -e "${BLUE}Step 4/7: Checking TODO comments...${NC}"
if [ -f "scripts/check-todos.sh" ]; then
    if ! ./scripts/check-todos.sh; then
        echo -e "${RED}❌ TODO check failed${NC}"
        FAILED=1
    else
        echo -e "${GREEN}✅ TODO check passed${NC}"
    fi
else
    echo -e "${YELLOW}⚠️ TODO checker not found, skipping${NC}"
fi

echo -e "${BLUE}Step 5/7: Checking bundle size...${NC}"
# Only run build check if package.json has build script
if npm run build &>/dev/null; then
    BUNDLE_SIZE=$(find dist -name "*.js" -exec wc -c {} + | tail -1 | awk '{print $1}')
    MAX_SIZE=2097152  # 2MB in bytes

    if [ "$BUNDLE_SIZE" -gt "$MAX_SIZE" ]; then
        echo -e "${RED}❌ Bundle size too large: $(($BUNDLE_SIZE / 1024))KB > $(($MAX_SIZE / 1024))KB${NC}"
        FAILED=1
    else
        echo -e "${GREEN}✅ Bundle size OK: $(($BUNDLE_SIZE / 1024))KB${NC}"
    fi
else
    echo -e "${YELLOW}⚠️ Build failed, skipping bundle size check${NC}"
fi

echo -e "${BLUE}Step 6/7: Cross-platform consistency check...${NC}"
if [ -f "../shared/scripts/validate-consistency.sh" ]; then
    if ! ../shared/scripts/validate-consistency.sh --quiet; then
        echo -e "${YELLOW}⚠️ Consistency check found issues (not blocking)${NC}"
    else
        echo -e "${GREEN}✅ Consistency check passed${NC}"
    fi
else
    echo -e "${YELLOW}⚠️ Consistency checker not found, skipping${NC}"
fi

echo -e "${BLUE}Step 7/7: Checking for console logs...${NC}"
if grep -r "console\." src/ --include="*.ts*" | grep -v "console.error\|console.warn" | head -5; then
    echo -e "${YELLOW}⚠️ Console logs found (please remove for production)${NC}"
else
    echo -e "${GREEN}✅ No console logs found${NC}"
fi

# Summary
echo ""
echo "============================================"
if [ $FAILED -eq 0 ]; then
    echo -e "${GREEN}🎉 All quality checks passed!${NC}"
    echo -e "${GREEN}✅ Commit approved${NC}"
    exit 0
else
    echo -e "${RED}💥 Quality checks failed!${NC}"
    echo -e "${RED}❌ Commit blocked${NC}"
    echo ""
    echo "Please fix the issues above and try again."
    exit 1
fi
